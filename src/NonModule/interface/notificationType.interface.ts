export interface notificationType {
  id: number;
  name: string;
  createdAt: Date;
}

export interface newNotificationType {
  name: string;
}

export interface editNotificationType {
  id: number;
  name: string;
}
